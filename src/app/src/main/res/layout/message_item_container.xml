<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="MissingDefaultResource">

    <data>

        <import type="android.view.View" />

        <variable
            name="time"
            type="String" />

        <variable
            name="title"
            type="String" />

        <variable
            name="selected"
            type="Boolean" />

        <variable
            name="onClickListener"
            type="View.OnClickListener" />

        <variable
            name="onLongClickListener"
            type="View.OnLongClickListener" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@{selected ? @color/selected : @android:color/white}"
        android:orientation="vertical"
        android:padding="8dp"
        app:setOnClickListener="@{onClickListener}"
        app:setOnLongClickListener="@{onLongClickListener}">

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/time_text_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:lines="1"
            android:text="@{time}"
            android:textColor="@color/darkPrimaryTextColor"
            android:textSize="12sp"
            tools:text="Title Title Title Title Title Title Title" />

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/title_text_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:autoLink="all"
            android:text="@{title}"
            android:textColor="@android:color/black"
            android:textSize="20sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Title Title Title Title Title Title Title" />
    </LinearLayout>
</layout>
